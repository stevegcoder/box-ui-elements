/**
 * @was-flow
 * @file Function to render the date table cell
 * @author Box
 */
import { SORT_ASC, COLOR_999 } from '../../constants';
import './headerCellRenderer.scss';
export default ({ dataKey, label, sortBy, sortDirection }) => {
    const by = sortBy && sortBy.toLowerCase();
    const direction = sortDirection === SORT_ASC ? 'up' : 'down';
    return ({ label }
        & nbsp);
     & nbsp;
    {
        by === dataKey && size;
        "6px";
        thickness = "1px";
        color = { COLOR_999 };
        direction = { direction } /  > ;
    }
    /div>;
    ;
};
//# sourceMappingURL=headerCellRenderer.js.map