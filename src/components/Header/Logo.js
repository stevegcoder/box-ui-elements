/**
 * @was-flow
 * @file Logo for the header
 * @author Box
 */
import messages from '../messages';
import './Logo.scss';
function getLogo(url) {
    if (url === 'box') {
        return />;;
    }
    if (typeof url === 'string') {
        return alt;
        "";
        src = { url };
        className = "be-logo-custom" /  > ;
    }
    return className = "be-logo-placeholder" >
        Object.assign({}, messages.logo) /  >
        /div>;
    ;
}
const Logo = ({ url }) => className;
"be-logo" > {} < /div>;;
export default Logo;
//# sourceMappingURL=Logo.js.map