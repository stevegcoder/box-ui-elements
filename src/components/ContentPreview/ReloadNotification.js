/**
 * @was-flow
 * @file Preview loading and error UI wrapper
 * @author Box
 */
import messages from '../messages';
import './ReloadNotification.scss';
const ReloadNotification = ({ onClick, onClose }) => className = "bcpr-notification" >
    type, { NotificationConstants, TYPE_INFO }, onClose = { onClose } >
    Object.assign({}, messages.previewUpdate) /  >
    onClick, { onClick };
 >
    Object.assign({}, messages.reload) /  >
    /Button>
    < /Notification>
    < /span>;
;
export default ReloadNotification;
//# sourceMappingURL=ReloadNotification.js.map