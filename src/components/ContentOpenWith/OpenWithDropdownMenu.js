/**
 * @was-flow
 * @file Open With dropdown menu
 * @author Box
 */
import Menu from 'box-react-ui/lib/components/menu/Menu';
const OpenWithDropdownMenu = ({ integrations, onClick }) => (isRightAligned >
    />
    < Menu), className = "bcow-menu" >
    { integrations } &&
    integrations.map(integration => integration = { integration }, onClick = { onClick }, key = { integration, : .appIntegrationId }
        /  >
    );
/Menu>
    < /DropdownMenu>;
;
export default OpenWithDropdownMenu;
//# sourceMappingURL=OpenWithDropdownMenu.js.map