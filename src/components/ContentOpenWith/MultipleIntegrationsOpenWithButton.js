/**
 * @was-flow
 * @file Open With button when multiple integrations are present
 * @author Box
 */
import Button from 'box-react-ui/lib/components/button/Button';
import { FormattedMessage } from 'react-intl';
import { CLASS_INTEGRATION_ICON } from '../../constants';
const MultipleIntegrationsOpenWithButton = (buttonProps) => text = {} < FormattedMessage, _a = void 0, { defaultOpenWithDescription } = _a;
/>} position="middle-left">
    < Button;
{
    buttonProps;
}
 >
    height;
{
    26;
}
width = { 26:  };
className = { CLASS_INTEGRATION_ICON } /  >
    /OpenWithButtonContents>
    < /MenuToggle>
    < /Button>
    < /Tooltip>;
;
export default MultipleIntegrationsOpenWithButton;
//# sourceMappingURL=MultipleIntegrationsOpenWithButton.js.map