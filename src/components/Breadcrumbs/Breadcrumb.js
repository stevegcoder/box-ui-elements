/**
 * @was-flow
 * @file Clickable breadcrumb component
 * @author Box
 */
import './Breadcrumb.scss';
const Breadcrumb = ({ name = '', onClick, isLast, delimiter }) => {
    const title = onClick ? type = "button" : , onClick = { onClick } >
        { name }
        < /PlainButton>;
};
({ name } < /span>);
return className = "be-breadcrumb" >
    { title };
{
    isLast ? null : delimiter;
    {
        delimiter;
    }
    />}
        < /span>;
    ;
}
;
export default Breadcrumb;
//# sourceMappingURL=Breadcrumb.js.map