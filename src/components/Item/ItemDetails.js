/**
 * @was-flow
 * @file Component for the details for the item name
 * @author Box
 */
import { VIEW_SEARCH, VIEW_SELECTED } from '../../constants';
import './ItemDetails.scss';
const ItemDetails = ({ view, rootId, item, onItemClick }) => className = "be-item-details" >
    { view } === VIEW_SELECTED || view === VIEW_SEARCH ? rootId = { rootId } : , item = { item }, onItemClick = { onItemClick } /  >
;
view = { view };
item = { item } /  >
;
/div>;
;
export default ItemDetails;
//# sourceMappingURL=ItemDetails.js.map