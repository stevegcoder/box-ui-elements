/**
 * @was-flow
 * @file Function to render the name table cell
 * @author Box
 */
import { VIEW_SEARCH } from '../../constants';
import './NameCell.scss';
export default (rootId, view, onItemClick, onItemSelect, canPreview = false, showDetails = true, isTouch = false) => ({ rowData }) => className = "be-item-name" >
    isTouch;
{
    isTouch;
}
item = { rowData };
canPreview = { canPreview };
onClick = { onItemClick };
onFocus = { onItemSelect }
    /  >
    { view } === VIEW_SEARCH || showDetails ? item = { rowData } : ;
view = { view };
rootId = { rootId };
onItemClick = { onItemClick } /  >
;
null;
/div>;
;
//# sourceMappingURL=nameCellRenderer.js.map