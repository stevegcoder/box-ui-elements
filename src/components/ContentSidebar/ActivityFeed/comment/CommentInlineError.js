/**
 * @was-flow
 * @file CommentInlineError component used by Comment component.
 */
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0)
            t[p[i]] = s[p[i]];
    return t;
};
import { FormattedMessage } from 'react-intl';
const CommentInlineError = ({ action, message, title }) => className = "bcs-comment-error", title = {} < FormattedMessage, title = __rest(void 0, []);
/>}>
    < div > {} < FormattedMessage;
{
    message;
}
/>}</div >
    {}
    < /InlineError>;
;
export default CommentInlineError;
//# sourceMappingURL=CommentInlineError.js.map