/**
 * @was-flow
 * @file Skills card component
 * @author Box
 */
import { SKILLS_TRANSCRIPT, SKILLS_KEYWORD } from '../../../constants';
const SidebarSkillsCard = ({ card, cards, hasError, isEditable, onSkillChange, getViewer }) => {
    switch (card.skill_card_type) {
        case SKILLS_KEYWORD:
            return card = { card };
            hasError = { hasError };
            transcript = {
                isEditable, cards, : .find(({ skill_card_type }) => skill_card_type === SKILLS_TRANSCRIPT)
            };
    }
};
undefined;
isEditable = { isEditable };
getViewer = { getViewer };
onSkillChange = { onSkillChange }
    /  >
;
;
SKILLS_TIMELINE: ;
SKILLS_FACE: return card = { card };
hasError = { hasError };
isEditable = { isEditable };
getViewer = { getViewer };
onSkillChange = { onSkillChange }
    /  >
;
;
SKILLS_TRANSCRIPT: return card = { card };
hasError = { hasError };
getViewer = { getViewer };
isEditable = { isEditable };
onSkillChange = { onSkillChange }
    /  >
;
;
SKILLS_STATUS: return card;
{
    card;
}
/>;;
return null;
;
export default SidebarSkillsCard;
//# sourceMappingURL=SidebarSkillsCard.js.map