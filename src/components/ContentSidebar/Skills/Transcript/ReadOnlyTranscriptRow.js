/**
 * @was-flow
 * @file Read only transcript row component
 * @author Box
 */
const ReadOnlyTranscriptRow = ({ time, text = '', onClick, interactionTarget }) => type = "button", className = "be-transcript-row", data;
-resin - target;
{
    interactionTarget;
}
onClick = { onClick } >
    { time } && className;
"be-transcript-time" > { time } < /div>}
    < div;
className = "be-transcript-text" > { text } < /div>
    < /PlainButton>;
;
export default ReadOnlyTranscriptRow;
//# sourceMappingURL=ReadOnlyTranscriptRow.js.map