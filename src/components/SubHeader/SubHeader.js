/**
 * @was-flow
 * @file Content sub header component
 * @author Box
 */
import './SubHeader.scss';
const SubHeader = ({ rootId, rootName, onItemClick, onSortChange, currentCollection, onUpload, onCreate, canUpload, canCreateNewFolder, view, isSmall, }) => className = "be-sub-header" >
    rootId, { rootId };
rootName = { rootName };
onItemClick = { onItemClick };
currentCollection = { currentCollection };
view = { view };
isSmall = { isSmall }
    /  >
    view;
{
    view;
}
currentCollection = { currentCollection };
canUpload = { canUpload };
canCreateNewFolder = { canCreateNewFolder };
onUpload = { onUpload };
onCreate = { onCreate };
onSortChange = { onSortChange }
    /  >
    /div>;
;
export default SubHeader;
//# sourceMappingURL=SubHeader.js.map